Compile with something like `gcc test.c -lpthread` and run with `./a.out`
You can pass arguments to the executable:
A: run only CPU-intensive threads
B: run only sleeping threads
C: run a uniform mixture of the two
